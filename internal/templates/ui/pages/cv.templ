package pages

import "context"

templ CV(ctx context.Context, htmlContent string) {
    <article class="cv-page px-4 sm:px-6 py-12 sm:py-16 max-w-3xl mx-auto">
        <div class="cv text-left text-foreground">
            @templ.Raw(htmlContent)
        </div>
    </article>
}
