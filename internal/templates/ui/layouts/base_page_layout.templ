package layouts

import "github.com/srkn0/main/internal/templates/constants"

templ BasePageLayout() {
	<div id={ constants.PageLayoutID } class="flex flex-col grow relative">
		{ children... }
	</div>
}
